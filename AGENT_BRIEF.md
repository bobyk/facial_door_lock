# ТЗ: прошивка автоматичного замка гаража на ESP32-S3

## Роль
Ти — вбудований (embedded) розробник C++/Arduino-framework для ESP32-S3. Пиши короткий, чистий, ідіоматичний код. Не вигадуй специфікації апаратних протоколів, яких немає в цьому документі — якщо чогось не вистачає, познач `// TODO:` з описом, що саме треба уточнити, замість здогадок.

## Ціль проєкту
Автоматичний замок гаражних дверей: відмикання по розпізнаванню обличчя, PIN-коду або (в перспективі) BLE-мітці; автоматичне замикання по таймеру; відмикання зсередини при наближенні людини до дверей.

## Апаратна конфігурація (підтверджена)

| Компонент | Модель | Інтерфейс | Примітка |
|---|---|---|---|
| MCU | ESP32-S3 | — | 2 апаратні I2C, 3 UART |
| Розпізнавання обличчя | HLK-FRM1213 (wl-frw121x) | UART, 3.3V | Дистанція 0.4–1.0м, <1с розпізнавання, до 100 облич. Протокол отримано від продавця (ML FRM/MRM series User Agreement Document V1.1.2) і реалізовано в `FRM1213Recognizer` |
| Клавіатура | Матриця 4x3 через PCF8574T | I2C (0x20 за замовч.) | P0-P3 рядки (вихід), P4-P6 стовпці (вхід, вбудований pull-up) |
| RTC | DS3231 | I2C | Бібліотека RTClib |
| LED-індикація | WS2812 стрічка | 1-Wire (data pin) | Керування через FastLED, пін фіксований на етапі компіляції |
| Датчик присутності (зовнішній) | LD2420 Radar Module | UART | 24Ghz |
| Датчик присутності (внутрішній) | VL53L1X (ToF) | I2C (0x29 за замовч.) | Той самий клас, окрема інстанція, поріг ~1000мм |
| Драйвер актуатора | 2-канальний (H-міст/реле) | GPIO x2 (IN1/IN2) | Керує автомобільним актуатором замка дверей, імпульсне керування |

Обґрунтування вибору ToF (а не мікрохвильового радара) для внутрішнього датчика: мікрохвильові радари (LD2410/LD2450) проникають крізь немeталеві стіни, що для внутрішнього тригера відмикання неприйнятно. VL53L1X — лазерний оптичний дальномір, вузький кут огляду (27°), фізично не бачить через стіни.

## Архітектура: два фізичні блоки

**Зовнішній блок** (на вулиці/біля дверей): клавіатура (PCF8574), камера FRM1213, LED-стрічка, зовнішній ToF-датчик присутності.

**Внутрішній блок** (всередині приміщення, захищений): ESP32-S3 (мозок), драйвер актуатора, RTC, внутрішній ToF-датчик присутності.

Обмеження, які треба врахувати в реалізації/коментарях коду:
- I2C між блоками надійний лише на короткій відстані (орієнтовно до ~1–1.5м на 100кГц з екранованою парою й підсиленими pull-up ~2.2кОм). Якщо кабель довший — потрібен буфер шини (напр. PCA9615) або локальний co-MCU в зовнішньому блоці з передачею даних по UART/RS485. Явно зазнач цей trade-off коментарем там, де ініціалізується I2C.
- Один VL53L1X мають однакову дефолтну адресу 0x29. Якщо вони на одній I2C-шині — обов'язково реалізувати переадресацію через пін XSHUT при ініціалізації (спочатку тримати XSHUT одного в LOW, ініціалізувати другий і призначити йому нову адресу, потім підняти перший). Якщо на різних апаратних I2C ESP32-S3 — конфлікту немає, зазнач, який варіант обрано.
- Актуатор — індуктивне навантаження (зворотний ЕРС) в тому ж корпусі, що й MCU (внутрішній блок). У коді це не вирішується, але додай коментар/попередження в `ActuatorDriver`, що на платі обов'язкова гальванічна розв'язка (оптрон) і снаббер/діод — це апаратна, не програмна вимога.

## Поведінкова специфікація (FSM)

Стани: `IDLE → SCANNING → AWAIT_CODE → LOCKOUT`, і прямий вихід у "відкрито" з будь-якого з перших трьох.

1. **IDLE**: очікування.
   - Якщо спрацював **внутрішній** датчик присутності (людина підійшла до дверей зсередини) → одразу `unlock()`, без сканування/коду.
   - Якщо спрацював **зовнішній** датчик присутності (людина в межах ~1м) → перехід у `SCANNING`.

2. **SCANNING**:
   - Запуск розпізнавання обличчя з таймаутом **1.5с**.
   - Якщо розпізнано → `unlock()`.
   - Якщо не розпізнано і людина досі в межах зовнішнього датчика → повторити скан.
   - Якщо не розпізнано і людина відійшла → повернення в `IDLE`.
   - Після **3 невдалих спроб поспіль** → перехід у `AWAIT_CODE`, LED починає **миготіти червоним** (сигнал "введіть пароль").

3. **AWAIT_CODE**:
   - Ввід цифр з клавіатури. Під час вводу LED **поступово заповнюється** кольором пропорційно введеним/залишковим символам коду.
   - Кнопка **`*`** — очистити введені цифри (скинути прогрес LED).
   - Кнопка **`#`** — примусово замкнути замок (`forceLock()`) з будь-якого стану.
   - При введенні повної довжини коду:
     - Якщо код правильний → скинути лічильники невдач → `unlock()`.
     - Якщо неправильний → інкремент лічильника невдач, очистити ввід.
       - Після **3 невдалих спроб поспіль** → `LOCKOUT`.

4. **LOCKOUT**:
   - LED показує **біжучий червоний** колір по всій довжині стрічки.
   - Ввід коду заблокований на час блокування.
   - Перше блокування: **3 хвилини**.
   - Якщо після зняття блокування знову введено 3 рази неправильно → нове блокування **вдвічі довше** за попереднє (експоненційне зростання: 3 → 6 → 12 → 24 хв...).
   - Успішне введення коду скидає лічильник стадій блокування до нуля.

5. **Успішне відмикання (`unlock()`)**, з будь-якого сценарію (обличчя/код/внутрішній датчик):
   - LED: **біжучий зелений знизу вгору** по стрічці.
   - Паралельно — сигнал на драйвер актуатора: відкрити замок.

6. **Автоматичне замикання**: крон-задача кожні **N хвилин** (параметр, за замовч. 10) викликає примусове замикання (`forceLock()`), незалежно від поточного стану.

## Вимоги до коду
- Кожен апаратний модуль — окремий клас/бібліотека (Arduino IDE `lib/`), без залежностей від бізнес-логіки замка, щоб можна було переносити на інші проєкти.
- Де можливо — інтерфейс (`IPresenceSensor`, `IFaceRecognizer`) + конкретна реалізація, щоб апаратну реалізацію можна було підміняти, не чіпаючи FSM.
- FSM (`LockController`) не блокуючий, крім свідомо задокументованих коротких блокуючих ділянок (напр. імпульс актуатора 50мс) — якщо агент рефакторить, ці ділянки мають лишатись помітними в коді.
- Мінімум коментарів, але кожне архітектурне рішення/обмеження (з розділу вище) — обов'язково відображене коментарем в місці, де це актуально.
- Не додавати нових зовнішніх залежностей без потреби; наявні: `RTClib`, `FastLED`, `VL53L1X` (Pololu).

## Відомі невідомі — НЕ вигадувати, позначати TODO
- Спосіб призначення адрес для двох VL53L1X — рішення (XSHUT-мультиплексування чи роздільні шини) ще не прийняте, обрати й задокументувати при імплементації.
- **Точний бінарний протокол кадрів LD2420** (заголовки, коди стану цілі/дистанції, checksum) — публічно не звірений і в цьому ТЗ не наведений. За аналогією з FRM1213 — `LD2420PresenceSensor` лишається заглушкою (UART відкривається, кадри вичитуються, але `isPresent()` завжди повертає `false`), доки протокол не буде підтверджений офіційним даташитом Hi-Link або сніфінгом UART. Альтернатива без парсингу протоколу: пін OT1 модуля видає готовий цифровий сигнал присутності.
- Точна довжина кабелю між зовнішнім і внутрішнім блоком — впливає на вибір I2C-буфера; уточнити перед фінальним підключенням.

## Стартова точка
Наявний каркас проєкту (Arduino IDE, ESP32-S3 super mini) з класами `RTCModule`, `KeypadPCF8574`, `LedStrip`, `ActuatorDriver`, `PresenceSensor` (`IPresenceSensor`/`ToFPresenceSensor`/`LD2420PresenceSensor`), `FaceRecognizer` (`IFaceRecognizer`/`FRM1213Recognizer`), `LockController`, `CronScheduler` — розширювати його, не переписувати з нуля.
